# Secrets — never commit
.env
*.pem
*.key

# Runtime state (per-machine watermarks, not shared)
state/*.json

# Generated reviews
reviews/*.md
reviews/*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# prime-agent session artifacts
.prime/sessions/
.prime/logs/

# Knowledge graphs are TRACKED (owner's choice, for convenience): they contain
# file paths of the reviewed private repos, so if this repo ever gets a remote,
# it must be private.

# Stray pytest capture files from subagent runs
pytest_stdout.txt
pytest_stderr.txt

# OS
Thumbs.db
.DS_Store
# Windows artifact from a stray `2>nul` redirect in a shell command
nul
